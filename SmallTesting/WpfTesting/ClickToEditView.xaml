<UserControl x:Class="SmallTesting.WpfTesting.ClickToEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wpfTest="clr-namespace:SmallTesting.WpfTesting"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Focusable="True" IsHitTestVisible="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <wpfTest:EditableTextBlock TextBlockBackgroundColor="White" TextBlockForegroundColor="Black" TextBoxBackgroundColor="White" TextBoxForegroundColor="Black"/>
        </Grid>
        <TextBox Grid.Row="1"/>

        <Grid Grid.Row="2">
        </Grid>
    </Grid>
</UserControl>
